namespace Mldong.Jeeflow.Persist;

/// <summary>
/// Persist 模块占位（M3 实现 DynamicTableWriter + PersistPostInterceptor + MetaTableReader，
/// 方案 §3.1 行 3 / T9 全规则）。当前仅承载程序集。
/// </summary>
public static class ModuleInfo
{
    public const string Name = "Mldong.Jeeflow.Persist";
}
